# ============================================
# Demonstração de import em Fig
# ============================================

import "matematica"

# Usando funções importadas
print(soma(10, 20));
print(mult(5, 6));
print(fatorial(5));

# Usando variável importada
print(PI);

# Usando em expressões
let area = mult(PI, mult(5, 5));
print(area);
