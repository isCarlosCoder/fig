# ============================================
# Módulo de utilidades matemáticas
# ============================================

fn soma(a, b) {
  return a + b;
}

fn mult(a, b) {
  return a * b;
}

fn fatorial(n) {
  if (n <= 1) { return 1; }
  return n * fatorial(n - 1);
}

let PI = 3.14159;
